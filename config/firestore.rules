rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for anyone (bypass authentication for now)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
